@model AppRH.Models.Return

@{
    Layout = "_Layout";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Create</title>
</head>
<body>

<!-- Modal -->
<div class="modal fade" id="staticBackdrop" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="staticBackdropLabel">Seleccionar Casa</h5>
      </div>
      <div class="modal-body">
         <select id="HouseID" class ="form-control" asp-items="ViewBag.HouseID"></select>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
        <a type="button" class="btn btn-primary" onclick="AddHouseReturn();">Agregar</a>
      </div>
    </div>
  </div>
</div>


<h4>Nueva Devolución</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ReturnDate" class="control-label"></label>
                <input asp-for="ReturnDate" class="form-control" />
                <span asp-validation-for="ReturnDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CustomerID" class="control-label"></label>
                <select asp-for="CustomerID" class ="form-control" asp-items="ViewBag.CustomerID"></select>
            </div> <br>
            <div class="form-group">
                <input type="submit" value="Confirmar Devolución" class="btn btn-primary" />
                <a type="button" onclick="CancelReturn();" class="btn btn-secondary" asp-action="Index">Cancelar Devolución</a>
            </div> <br>
        </form>
    </div>
</div>

<table class="table table-dark table-sm">
    <thead>
        <tr>
            <th>Casas</th>
            <th>
              <!-- Button trigger modal -->
                <div class=" d-flex justify-content-end">
                <button type="button" class="btn btn-light" data-bs-toggle="modal" data-bs-target="#staticBackdrop">
                Agregar Casa
                </button>
                </div>

            </th>
        </tr>
    </thead>
    <tbody id="tableHouseReturn"></tbody>
</table>


</body>
</html>
